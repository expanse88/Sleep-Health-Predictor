# -*- coding: utf-8 -*-
"""Sleep_Health_Prediction_Model.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xod6zfx-2hoUHEq8f341VBMF8ZEc2lNo
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.preprocessing import LabelEncoder

df= pd.read_csv("Sleep_health_and_lifestyle_dataset.csv")
df = df.drop('Person ID', axis=1)

le = LabelEncoder()
df['Sleep Disorder'] = le.fit_transform(df['Sleep Disorder'])

from sklearn.model_selection import train_test_split
X = df.drop('Sleep Disorder',axis=1)
y = df['Sleep Disorder']

X_train,X_test,y_train,y_test = train_test_split(X,y,train_size=0.2,random_state=42)

df.columns

sns.countplot(x='Sleep Disorder', data=df)
plt.title('Class Distribution: Sleep Disorder')
plt.show()

plt.figure(figsize=(14,8))
sns.heatmap(df.corr(),annot=True,cmap='Blues')
plt.title("Correlation Heatmap")
plt.show()


sns.boxplot(x='Sleep Disorder', y='Stress Level',data=df)
plt.title('Stress Level vs Sleep Disorder')
plt.show()

from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import classification_report

model = RandomForestClassifier()
model.fit(X_train,y_train)
y_pred = model.predict(X_test)
print(classification_report(y_test,y_pred))

